import Layout from '@/components/admin/Layout';
import { getServerSideProps } from '@/lib/auth';

export default function Eventos() {
  return <Layout>Eventos</Layout>;
}

export { getServerSideProps }
